<%= form_for(@guest) do |f| %>
<% if @guest.errors.any? %>
<div id="errorExplanation">
	<h2><%= pluralize(@guest.errors.count, "error") %> prohibited
	this post from being saved:</h2>
	<ul>
		<% @guest.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="field">
	<%= f.label :name %>
	<br />
	<%= f.text_field :name %>
</div>
<div class="field">
	<%= f.label :birth %>
	<br />
	<%= f.text_field :birth %>
</div>
<div class="field">
	<%= f.label :mother %>
	<br />
	<%= f.text_area :mother %>
</div>
<div class="field">
	<%= f.label :address %>
	<br />
	<%= f.text_area :address %>
</div>
<div class="field">
	<%= f.label :city %>
	<br />
	<%= f.text_area :city %>
</div>
<div class="field">
	<%= f.label :postal_code %>
	<br />
	<%= f.text_area :postal_code %>
</div>
<div class="field">
	<%= f.label :phone %>
	<br />
	<%= f.text_area :phone %>
</div>
<div class="field">
	<%= f.label :email %>
	<br />
	<%= f.text_area :email %>
</div>

<div class="actions">
	<%= f.submit :value => 'Edit guest', :class => 'submit' %>
</div>
<% end %>
<%= link_to 'Delete guest', guest_path(:id => @guest.id), :method => 'delete', :class => 'ui-icon ui-icon-trash' %>