<% unless (params[:guest_id]) %>

<%= link_to 'Select guest', guests_path(:mode => 'select', :from => new_examination_path) %>
<% end %>

<%= form_for(@examination) do |f| %>
<% if @examination.errors.any? %>
<div id="errorExplanation">
	<h2><%= pluralize(@examination.errors.count, "error") %> prohibited
	this post from being saved:</h2>
	<ul>
		<% @examination.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<h1>Add new examination</h1>
<%= f.hidden_field :guest_id, :value => @guest_id %>
<p>
<div class="field">
	<%= f.label :anamnezis %>
	<br />
	<%= f.text_field :anamnezis %>
</div>
<div class="field">
	<%= f.label :status %>
	<br />
	<%= f.text_field :status %>
</div>
<div class="field">
	<%= f.label :treatment %>
	<br />
	<%= f.text_field :treatment %>
</div>
<div class="field">
	<%= f.label :charge %>
	<br />
	<%= f.text_field :charge %>
</div>

<div class="actions">
	<%= f.submit :value => 'Add examinations', :class => 'submit' %>
</div>
<% end %>

